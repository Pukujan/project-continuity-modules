Astra owns planning, research, verification, and acceptance for project-continuity-modules. Kilo and Grok execute only an unconsumed TASK file Astra released in D:\claude\_workspace\pcm-astra-owner\inbox. If none exists, do not edit the product, do not open a product pull request, and do not choose the next slice.

A quiet, idle, exited, rejected, or interrupted Astra is not permission to implement. It is also not a reason to stop. Inspect the receipt, event log, and working tree, then repair the failure and get Astra running again. A local timeout after an accepted resume means resume that same session with a timeout long enough to finish. A provider parameter rejection means adjust the request and retry, using a fresh shorter turn if the old payload is invalid. A status report is not the finished result while Astra is down.

After a released task is done, write the result file, return it to Astra, and keep Astra working until Astra writes the next task.

Exception, approved by Alex 2026-09-24 (fresh-session mode since 2026-09-24 evening): InferHub model_param_invalid / code 11133 on cb/gpt-6-astra. Resuming the failed thread replays the same rejected history and fails again, so launch-astra.ps1 and resume-astra.ps1 now retry on 11133 only by starting a FRESH session through launch-astra.ps1, at most 2 times per original launch, each in a new receipt folder (logic in astra-retry11133.ps1). The launcher writes a short bounded handoff prompt (continue-after-11133-<failed receipt>-r<n>.md) pointing Astra at its latest DECISION and OWNER-NEXT files, the inbox TASK/RESULT pairing, and the failed thread id; retry 2 uses a shorter prompt. The failed thread is never resumed. Every decision is logged to RETRY.md in the evaluated receipt ("mode: fresh-session", origin and previous receipt/thread), and every 11133 failure and retry outcome is logged to the IRE Operational Issue Ledger (warnings only, never blocking). Staff must not resume a failed thread by hand and must never retry any other error this way. While a chain is running (the failed receipt's RETRY.md says "decision: RETRY" and a newer receipt exists with an attempt.txt showing action=codex-exec-fresh-retry11133), do not relaunch; wait for the chain. Only when RETRY.md says "NO RETRY" (non-11133 error or 2 retries used) does the normal recovery rule above apply; if you then relaunch fresh, change the prompt wording, because a fresh first request can also be rejected for its prompt content (20260924T223109Z failed on its first 3-message request).

## Cheap staff runner (Alex 2026-09-24 ~8:20 PM ET)
When an unconsumed TASK-NN*.md appears in inbox\ (no matching RESULT-NN*.md, no live STAFF-RUNNER-INPROGRESS-NN.md):
run it with the existing thin Codex+InferHub wrapper, do not execute the TASK yourself:

  powershell.exe -NoProfile -WindowStyle Hidden -ExecutionPolicy Bypass -File "D:\claude\_workspace\pcm-astra-owner\staff-runner\run-staff-task.ps1" -TaskFile "<full path to TASK>"

Defaults: model cb/deepseek-v4.1-flash, bid caps under $0.10/1M, full privileges (`--sandbox danger-full-access`, `approval_policy=never`; no sandbox approval prompts), no wall-clock timeout. Idempotent: existing RESULT or a live in-progress marker skips the run. Detach it (Win32_Process Create) and keep the Astra watchdog loop moving. Do not invent a new framework around this. Astra plans; this runner builds.
