# holdout/ — harness only

This directory is **private to Astra and the holdout harness**.

- Staff (InferHub / Codex builders, Kilo, Grok workers, anyone running `staff-runner\run-staff-task.ps1`) **must not read, list, copy, or search** this tree.
- Staff access to holdout content is a **process reject** (contamination). Visible scoreboards land in `inbox\HOLDOUT-NN.md` only.
- Layout: `holdout\TASK-NN\` with `judge\` rubrics and thresholds. Never place holdouts in the product repo or in `inbox\TASK`.
- See `guides\JEV-JUDGE-TESTS.md` for how JEV judges these packs.

If you are a builder agent and you can see this file because a path was pasted into a TASK: **stop and report contamination** instead of using the contents.
