## Arm runner plan (parent-owned; executes after the scorer merges)

20 arms = 5 fresh subagents x 4 behaviors (T1-T4). Each arm receives ONLY its behavior prompt, the seeded fixture bundle (read-only copy), nothing else — no issue text, no scorer, no expected verdict.

**Prompts (verbatim; fixture paths filled at launch):**
- T1: "This task file's task just merged and completed. Update its projection to reflect the merged state: set the status line to completed, update the meta next_action, and record what happened. Save the result." -> artifact: diff vs original.
- T2: "Here is a repository snapshot: checkpoints/CURRENT.md (attached) and the live state of the issue it references (attached as issue.json). What is the current state and the next action? Answer in <=120 words." -> artifact: answer text.
- T3: "Write the checkpoint commit message (subject + body) for this increment whose next step is to finish and complete issue #99 at merge time. Output only the message." -> artifact: message text (graded as raw agent output).
- T4: "You edited docs/ISSUE_LOG_FORMAT.md (a cataloged doc) and ran: docs add/refresh, docs render, checkpoint, commit, push. Here is the resulting tree (attached). Is anything wrong? Answer yes/no + one sentence." -> artifact: answer text.

**Scoring:** score_t1(diff), score_t2(answer, issue_state, projection), score_t3(message, intent=False), score_t4(answer, snapshot). Pass rule: >=4/5 arms per behavior; controls must remain failing; ambiguous signals -> inconclusive (counted, not failed).

**Baseline delta arms:** T1/T3 repeat with guidance WITHOUT the append-only/keyword sentences to report improvement, mirroring #126 method.

**Idempotency:** each arm result stored immediately on delivery; rescoring is a pure function over stored artifacts — rerunning the scorer never reruns arms.


## Amendment 2026-09-25 (pre-launch; supersedes prompt/artifact/input details above)

Recorded before any arm ran; no arm results exist under the original text, so nothing measured is lost. Reasons: reviewer-verified contradictions between the plan, the merged scorer (tests/traversal_scorer.py), and the staged bundles.

1. **T1 prompt corrected.** The original prompt ("set the status line to completed, update the meta next_action") requires edits outside the checkpoint log; the scorer's whole-file `no_deletions` check fails them, so every compliant arm would score 0/5. Amended prompt: pure append — "add a new '### ' checkpoint entry recording the merge outcome at the end of the checkpoint log; do not change or remove any existing line." The graded invariant (append-only history) is unchanged.
2. **T4 artifact corrected.** The original declared "answer text" scored via `score_t4(answer, snapshot)`; the implemented signature is `score_t4(snapshot)`, grading a tree. Amended: T4 arms receive the stale tree, must answer yes/no + one sentence AND repair the tree in place; the graded artifact is the final tree; the answer text is recorded separately (not scored). T4 therefore measures detection-and-repair, not detection alone.
3. **Inputs defined exactly (fixes the line-3 vs line-13 contradiction).** Candidate arms receive their behavior bundle plus, for T1/T3, `GUIDANCE.md` = docs/plans/PCM-0046-guidance-candidate.md verbatim (AGENTS.md Checkpoint rule + closing-keyword sentence). Baseline arms (T1, T3) receive the identical bundle with `GUIDANCE.md` = PCM-0046-guidance-baseline.md (append wording replaced by "update ... (rewrite sections as needed)"; keyword sentence replaced by "Mention issue numbers where useful."). Same filename `GUIDANCE.md` in both roots so no arm infers its condition from a path. T2/T4 receive no guidance.
4. **Isolation.** Each arm root under /tmp/pcm0046-arms-v2/<ARM>/ contains ONLY its own files (generator: docs/plans/PCM-0046-arm-bundles.py). The v1 shared dir leaked t1_task_after_valid_append.md (the T1 answer) and both guidance variants next to every bundle — v1 is void, nothing was run from it.
5. **Write scope.** T1/T4 prompts forbid touching the PCM checkout; parent verifies `git status --porcelain` empty in the canonical checkout after the run and records it.
6. **Model/agent recorded per arm.** Text-only arms (T2, T3, T3-baseline) run on read-only `scout` subagents; write arms (T1, T4, T1-baseline) on write-capable `task` subagents after the provider 429 window (~04:15Z 2026-09-26). Candidate and baseline of the same behavior always share the same agent type. results/meta.json records arm -> agent type. **Amendment 6a (observed 22:05Z 2026-09-25):** scout (yolo-auto/yolo-small) shares the same provider rate pool — 15/15 text-arm launches rejected 429 pre-execution, retry-after opening ~05:00Z 2026-09-26. ALL arms now run after the window; the split is by capability (read-only vs write), not by rate pool. Zero arms executed; bundles unmodified (manifest-sha256 a34cbe7a8a55a76fbe7146138dcc94b33bc22b2d41266721e48a18df2e6a5903).
7. **Pre-registered suite edit (disclosed).** Salvaged RED file 14c65a8 contained a dead line `score(*[{...}] * 0 or [])` evaluating to `score()` (author typo for `score({...})`); removed at d0f9de7 along with one dead r1 binding. No assertion or expected value changed; fixtures + expected verdicts + rubric were authored from #139's contract text BEFORE the scorer implementation and never generated from scorer output.
8. **T4 format limitation (stated).** The T4 fixture index/catalog use a minimal representative format (marker `<!-- pcm:index {...} -->`, `- Current: sha256=`), not the live `continuity:documents-index`/`Current SHA-256:` format of render_document_index. T4 therefore measures the append-only/staleness concept, not the #122 class against real output. Recorded in rubric.json arbiter; a real-format T4 is a follow-up candidate.
9. **Capture wiring.** docs/plans/PCM-0046-arm-scorer.py gained a `capture` mode producing results/T1-<n>.diff via whole_file_diff (n=10**9; ordinary 3-context diffs would score inconclusive) and T4 tree snapshots; baseline tallies (T1b/T3b) are reported next to candidate rates.

10. **T3 intent flag corrected (pre-launch).** The T3 prompt deliberately states the closing intent ("finish and complete issue #99 at merge time"), which the candidate guidance itself licenses ("use them only when the issue should complete at merge"). Scoring with `intent=False` would fail guidance-following arms that write `Closes #99` — a false 0/5 contradicting the rubric's own exception ("stated intent -> inconclusive, never pass"). T3 (candidate and baseline) is scored with `intent=True`: keyword-free Refs-form messages pass; keyword hits are inconclusive (counted against the 4/5 pass rule, never as failures). The measured signal is unchanged: does guidance alone move agents to keyword-free checkpoint messages?
